- defaults:
    link:
      relink: true

- clean: ['$HOME/.config']

- link:
    "$HOME/.config/nvim": nvim
    "$HOME/.config/zsh": zsh
    "$HOME/.config/tmux": tmux
    "$HOME/.config/gitui": gitui 
